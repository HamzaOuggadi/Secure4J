spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/social4j
    username: hamzaouggadi
    password: hamza123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: social4j
server:
  port: 8081